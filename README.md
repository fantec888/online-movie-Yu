# 🎬 Online Movie House (Backend)

基于 Node.js 的在线多人同步观影系统后端服务。

## 📅 当前进度：第三次迭代 (v1.3)
**核心目标**：实现 Socket.IO 实时通信与消息广播。

### ✨ 本次迭代新增功能
1.  **Socket.IO 集成**：
    - 建立了 `/chat` 命名空间，专门处理聊天业务。
    - 实现了 WebSocket 连接管理与心跳检测。
2.  **消息网关 (ChatGateway)**：
    - `room:join`：处理用户加入房间频道。
    - `message:send`：接收用户消息并广播给同房间所有成员。
    - `message:new`：定义了标准的广播消息格式。
3.  **服务层升级 (RoomService)**：
    - 新增 `findById` 内部方法，支持 ChatService 获取房间实体对象。
    - 实现了从网关到业务层的完整调用链路。

---

## 🛠 技术栈
- **Runtime**: Node.js
- **Framework**: Express
- **Real-time**: Socket.IO
- **Architecture**: Layered Architecture (Controller/Gateway -> Service -> Repository)

## 🚀 快速开始

### 1. 安装依赖
```bash
npm install
